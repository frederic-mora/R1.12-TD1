/*  Note : ceci est un commentaire. C'est-à-dire que c'est une anotation à l'usage du dévelopeur. 
    Le navigateur ignore tous les commentaires puisque ce ne sont pas des instructions qu'il doit exécuter.
    Un commentaire sur plusieurs lignes est délimité par les caractères /* et * /
    Un commentaire sur une seule ligne est repérable pas un // en début de ligne.

    Modifier le code ci dessous pour faire apparaître vos nom, prénom et age.
    MAIS : ne modifiez pas le code déjà écrit. Ajoutez à la suite, les 
    instructions qui vont modifier la valeur des variables.
    Quand vous pensez avoir fait le nécessaire, testez vos modifications en cliquant
    sur le bouton "Test my code" présent sur le document html.

    Note : il n'y a pas de miracle, si la valeur de ces variables apparait sur la page web,
    c'est qu'un autre code se charge de l'y recopier. Il se trouve dans tool.js, mais vous
    n'avez pas à vous en occuper, ni encore les compétences pour le comprendre.
*/

// Déclaration d'une variable nom contenant la chaîne de caractères "Georges".
// Notez que le ; sert à délimiter/terminer chaque instruction.
let nom = "Georges";

// Déclaration de la variable prenom contenant la chaîne de caractères "Abitbol"
let prenom = "Abitbol";

// Déclaration de la variable age contenant le nombre 100
let age = 100;

// Ajoutez ici vos instructions


nom = "Mora";